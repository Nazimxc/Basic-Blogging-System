<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Post in Detail</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow">
                    <div class="card-header bg-info text-white">
                        <h4 class="text-center">Post Details</h4>
                    </div>
                    <div class="card-body">
                        <p class="text-muted"><strong>Post ID:</strong> <%= post.id %></p>
                        <h5 class="text-primary">@<%= post.username %></h5>
                        <p class="fs-5"><%= post.content %></p>
                    </div>
                    <div class="card-footer text-center">
                        <a href="/posts" class="btn btn-secondary">Back to Posts</a>
                        <a href="/posts/<%= post.id %>/edit" class="btn btn-warning">Edit Post</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
